const MyTag = ({ tagName }) => {
    return (
      <span className="inline-block bg-blue-400 rounded-full px-2 py-1 text-xs text-white mr-2 mb-2">
        {tagName}
      </span>
    );
  };

  export default MyTag;
